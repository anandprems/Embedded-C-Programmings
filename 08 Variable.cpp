#include <stdio.h>

int main() {
    int total_students = 50;
    float average_score = 75.5;

    printf("Total students: %d\n", total_students);
    printf("Average score: %.2f\n", average_score);

    return 0;
}

